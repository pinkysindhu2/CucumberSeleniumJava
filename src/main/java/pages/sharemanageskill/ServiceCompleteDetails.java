package pages.sharemanageskill;

import org.openqa.selenium.WebDriver;
import pages.account.Profile;

public class ServiceCompleteDetails extends Profile {

    public ServiceCompleteDetails(WebDriver driver) {
        super(driver);
    }
}
